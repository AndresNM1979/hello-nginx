user  nginx;
worker_processes  1;

error_log  /var/log/nginx/error.log warn;
#pid        /var/run/nginx.pid;

events {
  worker_connections  4096;  ## Default: 1024
}
http {
 server {
   listen 8080;
   listen [::]:8080;

    server_name spring-form2;
 
   location /lista {
       proxy_pass http://spring-form2:8080;
   }
    location /creado {
       proxy_pass http://spring-form2:8081;
   }
 }
}
